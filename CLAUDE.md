# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 프로젝트 가이드입니다. (초안이며, 프로젝트가 진행되면서 계속 보완되어야 합니다)

## 프로젝트 개요

구글 광고(Google Ads) 캠페인 원본 리포트를 분석하고, Streamlit으로 마케팅 성과 대시보드를 만드는 프로젝트입니다.

- 광고주: 홈트레이닝/홈핏 브랜드 (광고그룹: "홈트 클래스", "홈핏 브랜드")
- 전환 목표: 구독 신청
- 핵심 지표: 노출수, 클릭수, CTR, 평균 CPC, 비용, 구독 신청(전환수), 전환율, 전환당비용(CPA)
- 데이터 기간: 2026-07-06 ~ 2026-08-04 (약 30일)

## 개발 환경

- Python 3.14.6, 가상환경: `venv/`
  - Windows에서 `python` 명령이 MS Store 스텁(`WindowsApps\python.exe`)으로 연결되는 경우가 있으니, 새로 셋업할 때는 `py` 런처를 사용하세요 (`py -m venv venv`).
- 활성화: `.\venv\Scripts\Activate.ps1` (PowerShell). 스크립트 실행 정책 때문에 막히면 `.\venv\Scripts\python.exe`, `.\venv\Scripts\pip.exe`를 직접 호출하세요.
- 의존성 설치: `pip install -r requirements.txt`
- 설치된 주요 패키지: pandas, numpy, streamlit, plotly, matplotlib, openpyxl, python-dateutil
- 대시보드 실행: `streamlit run app.py` (기본 포트 8501)

## 데이터 (`data/raw/`)

원본 CSV는 절대 수정하지 않습니다. 전처리 결과물은 `data/processed/`에 저장합니다 (`.gitignore`에 포함되어 커밋되지 않음).

| 파일 | 내용 | 행 수 | 주요 컬럼 |
|---|---|---|---|
| `campaign_daily.csv` | 일별 캠페인 성과 | 30일 + 합계 1행 | 날짜, 요일, 노출수, 클릭수, CTR, 평균 CPC, 비용, 구독 신청, 전환율, 전환당비용, 예산 소진, 학습 기간 |
| `keywords.csv` | 키워드별 성과 | 8개 키워드 | 키워드, 광고그룹, 검색유형, 품질평가점수, 광고 관련성, 예상 CTR, 방문 페이지, 노출수, 클릭수, CTR, 평균 CPC, 비용, 구독 신청, 고객 아님 |
| `search_terms.csv` | 실검색어 리포트 | 12개 검색어 | 검색어, 관련도, 일치 키워드, 광고그룹, 검색유형, 노출수, 클릭수, CTR, 비용, 구독 신청, 전환당비용 |
| `device_hour.csv` | 기기/시간대별 성과 (한 파일에 두 차원 혼재) | 기기 3 + 시간대 24 | 구분("기기"/"시간대"), 값, 노출수, 클릭수, CTR, 비용, 구독 신청 |
| `ad_groups.csv` | 광고그룹 요약 | 2개 그룹 | 광고그룹, 키워드 수, 평균 품질평가점수, 노출수, 클릭수, CTR, 평균 CPC, 비용, 구독 신청 |
| `placements.csv` | 게재위치 성과 | 헤더만, 데이터 0건 | 게재위치, 관련도, 노출수, 클릭수, CTR, 비용, 구독 신청 |

## 데이터 처리 시 주의사항 (실제 파일 확인 결과 — 중요)

1. **`campaign_daily.csv` 마지막 행은 "합계" 총계 행입니다.** `날짜` 컬럼 값이 `"합계"`인 행이 그대로 들어있어, 필터링 없이 로드하면 날짜별 집계/그래프가 깨집니다. 로드 시 반드시 제외하세요. 예: `df = df[pd.to_datetime(df["날짜"], errors="coerce").notna()]`
2. **인코딩은 UTF-8 BOM(EF BB BF)** 입니다. 반드시 `pd.read_csv(path, encoding="utf-8-sig")`로 읽어야 컬럼명 앞에 `﻿`가 붙지 않습니다.
3. **퍼센트 컬럼은 문자열입니다.** `CTR`, `전환율`은 `"13.87%"` 형태의 문자열이며 숫자가 아닙니다. 계산에 쓰려면 `.str.rstrip("%").astype(float)` 변환이 필요합니다.
4. **결측치(빈 값)는 0이 아닙니다.** 전환이 0건인 날짜/키워드는 `전환당비용`이 빈 값(NaN)입니다 (0으로 나누기를 피하기 위한 것으로 보임). `fillna(0)`으로 뭉개면 "전환 0건"과 "값 없음"이 구분되지 않으니 주의하세요.
5. **`device_hour.csv`의 `구독 신청`은 소수 값을 가질 수 있습니다** (예: `0.38`, `6.50`). 기여(attribution) 모델에 따라 분배된 값으로 보이며, 정수 카운트로 가정하면 안 됩니다. 이 파일의 시간대별 합계가 `campaign_daily`의 일별 합계와 정확히 일치하지 않을 수 있습니다.
6. **`device_hour.csv`는 두 차원이 한 파일에 있습니다.** `구분` 컬럼이 `"기기"`(휴대전화/컴퓨터/태블릿)인지 `"시간대"`(00:00~23:00)인지로 먼저 나눠서 분석하세요.
7. **`placements.csv`는 현재 빈 데이터입니다.** 헤더만 있고 데이터 행이 없습니다. 대시보드 코드에서 빈 데이터프레임을 방어적으로 처리하세요 (추후 채워질 가능성 있음).
8. **통화 컬럼(`비용`, `평균 CPC`, `전환당비용`)은 콤마 없는 원화 정수**입니다. 천 단위 콤마 포맷팅은 화면 표시 단계에서만 적용하세요.
9. **일별 합산 값이 원본 "합계" 행과 완전히 일치하지 않을 수 있습니다.** 30일치 `비용`을 직접 합산하면 1,095,716원인데 원본 "합계" 행은 1,095,718원으로 2원 차이가 있습니다(구글 광고 자체 반올림 처리로 추정). 코드 버그가 아니니 합계 검증 시 몇 원 단위 오차는 정상 범위로 간주하세요.

## 폴더 구조

```
marketinig_dashboard/
├── venv/                        # 가상환경 (git 제외)
├── data/
│   ├── raw/                      # 원본 CSV, 수정 금지
│   └── processed/                # 전처리된 데이터 (git 제외, 현재 미사용)
├── src/
│   ├── data_loader.py            # CSV 로딩 + 인코딩/타입 정제
│   ├── metrics.py                # 가중 CTR/CPA/전환율, 요일별 집계
│   └── dashboard_common.py       # 3개 페이지가 공유하는 상수·캐시로더·필터·스타일 헬퍼
├── pages/
│   ├── 1_전환율.py                # 히어로 전환율 + 요일별 성과
│   ├── 2_노출_클릭_성과.py         # 트래픽 퍼널(노출→CTR→클릭) + 기기/시간대 + 검색어
│   └── 3_최종_측면.py              # 비용 효율(비용→구독신청→CPA) + 키워드 + 광고그룹
├── app.py                        # st.navigation 라우터 (페이지 등록만, UI 로직 없음)
├── requirements.txt
└── CLAUDE.md
```

## 페이지 구조 (멀티페이지 앱)

`app.py`는 `st.set_page_config` + `st.navigation([...])`만 하는 얇은 라우터이고, 실제 화면은
`pages/`의 3개 파일이 각각 담당합니다. 사이드바 상단에 Streamlit이 자동으로 페이지 링크
목록을 렌더링합니다 (`/`, `/노출_클릭_성과`, `/최종_측면` — 각각 독립된 URL).

- **1_전환율.py**: 핵심 요약 배너 + 전환율 히어로(대형 숫자) + 지표 추이(7개 탭) + 주별 추이(히트맵 표) + 요일별 성과. 기본(첫) 페이지이자 전체 지표를 한 번에 훑어보는 개요.
- **2_노출_클릭_성과.py**: 트래픽 퍼널 KPI(노출수→CTR→클릭수) + 지표별 추이 탭 + 기기/시간대 + 실검색어(신규·제외 키워드 후보)
- **3_최종_측면.py**: 비용 효율 KPI(비용→구독신청→CPA) + 지표별 추이 탭 + 키워드(비용·CPA) + 광고그룹 + 게재위치

세 페이지 모두 맨 위에서 `dc.render_sidebar()` → `dc.render_date_header(subtitle)` → `dc.PeriodContext(...)` 순서로
호출해서 동일한 필터 상태(날짜 범위·학습기간·목표 CPA)를 공유합니다. 사이드바 위젯에 명시적
`key`(`show_learning`, `target_cpa`, `date_range_filter`)를 줬기 때문에 Streamlit의
`st.session_state`를 통해 페이지를 이동해도 값이 유지됩니다 — **새 위젯을 추가할 때도 반드시
`key`를 명시하세요.** 안 그러면 페이지마다 별개 상태로 취급됩니다.

새 페이지를 추가할 때: `pages/`에 파일을 만들고 `app.py`의 `st.navigation([...])` 목록에
`st.Page("pages/파일명.py", title=...)`를 추가하면 됩니다. `st.set_page_config`는 `app.py`
한 곳에만 있어야 합니다(페이지 파일에서 또 호출하면 에러).

## 코딩 컨벤션

- 데이터 로딩/정제 로직은 `src/data_loader.py`에, 페이지 간 공유 로직(캐시 로더·필터·스타일)은
  `src/dashboard_common.py`에 두고, `pages/*.py`에는 해당 페이지 고유의 화면 구성만 둡니다.
- 컬럼명은 원본 한글을 그대로 유지합니다 (원본 리포트와 대조하기 쉽도록 번역하지 않습니다).
- 정제된 파생 컬럼은 원본과 구분되도록 접미사를 붙입니다. 예: `CTR`(원본 문자열) → `CTR_pct`(float)
- 새 차트/지표를 추가하기 전에 위 "데이터 처리 시 주의사항"을 먼저 확인하고 반영합니다.

## 분석/대시보드 관점 (구현 완료)

- **지표 추이**: 노출·CTR·클릭·전환율·구독신청·비용·CPA 각각의 일별 추이(페이지별로 관련 지표만), `학습 기간=Y` 구간 음영 표시, 선택 기간과 동일 길이의 직전 기간 대비 증감률(%) (`campaign_daily`)
- **요일별 성과**: `filtered`(기간·학습기간 필터 적용된 데이터)를 `metrics.aggregate_by_weekday()`로 월~일 집계. 표본 일수가 적은 요일은 편차가 클 수 있어 "일수" 컬럼을 함께 노출 (`campaign_daily`)
- **주별 추이**: `metrics.aggregate_by_week()`로 ISO 캘린더 주(월요일 시작) 단위 집계 후 `style_table()`로 히트맵 표 렌더링. 맨 앞/뒤 주는 7일이 안 채워진 부분 주일 수 있어 "일수" 컬럼 포함 (`campaign_daily`). 요일별(월~일 패턴, 시간 순서 없음)과 달리 이건 시간 순서가 있는 "추이"라 1_전환율.py에서 일별 지표 추이 바로 옆에 배치했습니다.
- **키워드 성과**: CTR·CPA 기준 정렬, 품질평가점수 vs CPA 산점도 (`keywords`)
- **검색어 인사이트**: `search_terms["검색어"]`가 `keywords["키워드"]`에 없으면 "신규 키워드 후보", 클릭은 있는데 전환 0건이면 "제외 키워드 후보"로 자동 분류 (`search_terms`)
- **기기/시간대 최적화**: 노출/클릭/비용/구독신청뿐 아니라 CTR·CPA도 선택해서 볼 수 있음 (`device_hour`)
- **광고그룹 요약**: 그룹별 CPA 바차트 (`ad_groups`)
- **목표 CPA**: 사이드바에서 목표 CPA(원)를 입력하면(기본값 0 = 비활성) KPI 카드, 일별 추이 차트, 요일별 차트, 키워드 표/산점도, 광고그룹 표/차트에 목표선(빨간 점선)과 초과 행 강조(빨간 배경)가 함께 반영됩니다.

이 중 키워드/검색어/기기시간대/광고그룹 탭은 원본 CSV 자체가 일자별이 아닌 기간 집계본이라 **사이드바의 기간 필터가 적용되지 않고 항상 전체 기간(`campaign_daily`의 min~max) 기준**입니다. 각 탭에 이를 알리는 캡션이 있습니다. (요일별 성과는 예외로, `campaign_daily` 기반이라 기간 필터가 정상 적용됩니다.)

## 가독성 (`src/dashboard_common.py`의 `style_table()`)

모든 표는 3개 페이지 공통으로 `style_table()` 헬퍼 하나를 거쳐 렌더링됩니다(중복 함수 없음):

- **컬럼 정리**: 항상 결측인 컬럼(`예산 소진`, `고객 아님`), 값이 하나뿐인 컬럼(`광고 관련성`·`예상 CTR`·`방문 페이지` — 전부 "평균 이상"), `_pct` float 컬럼과 중복되는 원본 % 문자열 컬럼(`CTR`, `전환율`)을 제거합니다. 원본 CSV에 새 컬럼이 추가되면 이 목록(`DROP_COLS`)에 없으므로 기본적으로 표시됩니다 — 새로 항상-결측/항상-동일 컬럼이 생기면 `DROP_COLS`에 추가하세요.
- **숫자 포맷**: `NUMBER_FORMATS` 딕셔너리로 통화(`₩{:,.0f}`)·퍼센트(`{:.2f}%`)·정수(`{:,.0f}`)를 컬럼명 기준으로 일괄 적용합니다.
- **히트맵**: `HEATMAP_HIGH_IS_GOOD`(CTR·전환율·구독신청·품질점수 — 높을수록 좋음 → `RdYlGn`)과 `HEATMAP_LOW_IS_GOOD`(CPA·비용 — 낮을수록 좋음 → `RdYlGn_r`)로 나눠 컬럼별 독립 정규화(`axis=0`) 그라데이션을 입힙니다. matplotlib이 없으면 `Styler.background_gradient()`가 실패하니 `requirements.txt`에서 matplotlib을 빼면 안 됩니다.
- **목표 CPA 강조**: 그라데이션 위에 `cpa_col` 인자를 넘긴 표만 목표 초과 행을 반투명 빨강으로 한 번 더 덮어씁니다(그라데이션보다 우선순위 높음).
- 새 표를 추가할 때는 원본 컬럼을 그대로 `dc.style_table(df, target_cpa, has_target, cpa_col=...)`에 넘기면 되고, 표시용 컬럼을 미리 골라낸 경우(예: 검색어 페이지의 후보 표)에도 그대로 통과시키면 됩니다. `target_cpa`/`has_target`은 `dc.render_sidebar()`가 반환한 값을 그대로 전달하세요(더 이상 전역 변수가 아니라 각 페이지가 명시적으로 들고 다닙니다).

## 색상 (`src/dashboard_common.py` 상수)

- `GROUP_COLORS = {"홈트 클래스": "#2a78d6", "홈핏 브랜드": "#eb6834"}` — 광고그룹 2개는 모든 차트(키워드 비용바, 품질점수 산점도, 광고그룹 CPA바)에서 이 매핑을 그대로 씁니다. 광고그룹이 3개 이상으로 늘어나면 `dataviz` 스킬의 `references/palette.md` 카테고리 팔레트에서 slot 3(`#1baf7a`)을 이어서 쓰고 `scripts/validate_palette.js`로 색맹 안전성을 재검증하세요.
- `TARGET_LINE_COLOR = "#d03b3b"` — 목표선/초과 강조에 쓰는 단일 경고색(같은 팔레트의 status critical 계열).

## 운영 관련 참고

- **캐시**: 로더 함수는 `src/dashboard_common.py`에서 `@st.cache_data`로 감싸 캐싱되며, 3개 페이지가 모두 같은 캐시를 공유합니다(멀티페이지 앱에서도 `st.cache_data`는 앱 전체 기준). `data/raw/`의 CSV를 새 리포트로 교체해도 프로세스가 살아있는 한 자동으로 재로딩되지 않으니, 사이드바의 "데이터 새로고침" 버튼(`st.cache_data.clear()` 호출, 아무 페이지에서나 누르면 전체에 반영)을 눌러야 합니다.
- **`날짜` 컬럼 파싱**: `load_campaign_daily()`는 `pd.to_datetime(..., errors="coerce")`로 날짜 형식이 아닌 행(예: "합계")을 걸러냅니다. 문자열 완전일치가 아니라 파싱 가능 여부로 판단하므로, 다음 리포트에서 집계 행 라벨이 바뀌어도(`총계` 등) 안전합니다.

## 확인이 필요한 항목 (TODO)

- 대시보드 배포 방식: 로컬 실행만 할지, Streamlit Community Cloud 등에 배포할지
- `placements.csv`가 언제 채워질 예정인지 (게재위치 데이터 소스 확인)
- 신규/제외 키워드 후보 로직을 실제 운영에 반영할 담당자·프로세스가 있는지 (현재는 대시보드에 후보만 표시, 자동 반영 없음)
